global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "adk-progress-bridge"
    static_configs:
      - targets: ["host.docker.internal:8000"] # For local development
    # If running inside docker-compose with the API:
    # - targets: ["api:8000"]
